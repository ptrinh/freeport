# Deprecated — merged into `packages/nostr-mcp`

The Web Push notifier now lives inside the MCP server (`packages/nostr-mcp/src/notify/`)
so both share one hostname (`nostr-mcp.trinh.uk`). This directory is no longer a
workspace and is not built or published. Safe to delete.
