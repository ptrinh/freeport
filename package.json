{
  "name": "freeport-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/protocol",
    "packages/agent",
    "packages/nostr-mcp"
  ],
  "scripts": {
    "build": "npm run build -w @freeport/protocol -w @freeport/agent -w freeport-nostr-mcp",
    "test": "npm run test -w @freeport/protocol -w @freeport/agent",
    "demo": "bash demo/run-demo.sh"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0"
  }
}
